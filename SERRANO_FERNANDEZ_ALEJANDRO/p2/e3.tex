\begin{lstlisting}
	for(int j = 0; j <= ases; j++)
	{
		if(j < p[0])
			f[0][j] = 0;
		else
			f[0][j] = v[0];
	}



	for(i = 1; i < N; i++)
	{
		for(int j = 0; j <= ases ; j++)
		{
			if(j < p[i])
				f[i][j] = f[i-1][j];
				
			else		
				f[i][j] = std::max(f[i-1][j],f[i-1][j - p[i]]+v[i]);
		}
	}

\end{lstlisting}